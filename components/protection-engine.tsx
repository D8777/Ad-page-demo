'use client'

import { useMemo, useState } from 'react'
import { ArrowRight, Check, ChevronDown, Clock3, Droplets, MapPin, MessageSquare, ShieldCheck, Sparkles, Sun, Phone, X } from 'lucide-react'
import { siteConfig } from '@/src/config/site-config'

const sizes = [{ label: 'Coupe / Sedan', add: 0 }, { label: 'Midsize SUV / Truck', add: 150 }, { label: 'Full-Size SUV / EV', add: 250 }]
const goals = [{ label: '3–5 Year Ceramic', index: 0 }, { label: 'Lifetime Ceramic Tint', index: 1 }, { label: 'PNW Winter Defense', index: 2 }]

export function ProtectionEngine() {
  const [size, setSize] = useState(0)
  const [goal, setGoal] = useState(0)
  const [addTint, setAddTint] = useState(false)
  const [submitted, setSubmitted] = useState(false)
  const [faq, setFaq] = useState<number | null>(0)
  const selected = siteConfig.packages[goal]
  const tintPrice = addTint && goal !== 1 ? siteConfig.tintAddOn.price : 0
  const estimate = useMemo(() => ({ min: selected.price + sizes[size].add + tintPrice, max: selected.price + sizes[size].add + tintPrice + (goal === 2 ? 400 : 300) }), [goal, selected.price, size, tintPrice])

  return <main className="min-h-screen overflow-hidden bg-[#0a0d14] text-slate-100">
    <header className="mx-auto flex max-w-6xl items-center justify-between px-5 py-5 lg:px-8">
      <div className="flex items-center gap-3"><div className="grid size-9 place-items-center rounded-lg bg-cyan-300 text-[#0a0d14]"><Sparkles size={18} /></div><div><p className="text-sm font-semibold tracking-tight">{siteConfig.shopName}</p><p className="text-[10px] uppercase tracking-[.22em] text-slate-500">Protection systems</p></div></div>
      <a href={`tel:${siteConfig.phone}`} className="hidden items-center gap-2 text-xs font-medium text-slate-300 transition hover:text-cyan-300 sm:flex"><Phone size={14} /> {siteConfig.phone}</a>
      <a href="#estimator" className="rounded-full border border-slate-700 px-4 py-2 text-xs font-medium text-slate-200 transition hover:border-cyan-400 hover:text-cyan-300">Get estimate <ArrowRight className="ml-1 inline" size={13} /></a>
    </header>

    <section className="relative mx-auto max-w-6xl px-5 pb-16 pt-10 lg:px-8 lg:pb-24 lg:pt-20"><div className="pointer-events-none absolute -right-40 top-0 size-[500px] rounded-full bg-cyan-400/10 blur-[120px]" />
      <div className="relative max-w-3xl"><div className="mb-6 inline-flex items-center gap-2 rounded-full border border-cyan-400/25 bg-cyan-400/10 px-3 py-1.5 text-[11px] font-medium text-cyan-200"><span className="size-1.5 rounded-full bg-cyan-300 shadow-[0_0_10px_#00d2ff]" /> {siteConfig.trustBadge}</div><h1 className="text-5xl font-semibold leading-[.98] tracking-[-.055em] text-white sm:text-6xl lg:text-8xl">Paint defense<span className="text-cyan-300">.</span><br />Engineered for<br /><span className="text-slate-400">{siteConfig.city} weather.</span></h1><p className="mt-7 max-w-xl text-base leading-7 text-slate-400">Skip the phone tag. Protect your paint, block road grime, and get an instant package estimate in under 60 seconds.</p><div className="mt-8 flex flex-wrap gap-3"><a href="#estimator" className="rounded-lg bg-cyan-300 px-5 py-3 text-sm font-semibold text-[#0a0d14] transition hover:bg-cyan-200">Calculate package estimate <ArrowRight className="ml-2 inline" size={16} /></a><a href={`sms:${siteConfig.phone}`} className="rounded-lg border border-slate-700 px-5 py-3 text-sm font-semibold text-slate-200 transition hover:border-slate-500"><MessageSquare className="mr-2 inline" size={16} /> Instant text-back</a></div></div>
      <div className="mt-14 grid grid-cols-1 gap-3 border-t border-slate-800 pt-5 sm:grid-cols-3"><Trust icon={<ShieldCheck />} text="Certified installers" /><Trust icon={<Sparkles />} text="Clean handoff guarantee" /><Trust icon={<Check />} text="Carfax-registered warranties" /></div>
    </section>

    <section id="estimator" className="border-y border-slate-800 bg-[#0d121c] px-5 py-16 lg:px-8 lg:py-24"><div className="mx-auto grid max-w-6xl gap-10 lg:grid-cols-[.8fr_1.2fr]"><div><p className="mb-3 text-xs font-semibold uppercase tracking-[.25em] text-cyan-300">01 — Find your fit</p><h2 className="text-4xl font-semibold tracking-[-.04em] text-white sm:text-5xl">Your car.<br /><span className="text-slate-500">Your defense plan.</span></h2><p className="mt-5 max-w-sm text-sm leading-6 text-slate-400">A transparent starting point, built around your vehicle and the weather it actually sees.</p><div className="mt-8 space-y-3">{['Vehicle profile', 'Protection objective', 'Intake handshake'].map((item, i) => <div key={item} className={`flex items-center gap-3 text-sm ${i === 0 ? 'text-white' : 'text-slate-600'}`}><span className={`grid size-7 place-items-center rounded-full text-xs ${i === 0 ? 'bg-cyan-300 text-[#0a0d14]' : 'border border-slate-700'}`}>0{i + 1}</span>{item}</div>)}</div></div>
      <div className="rounded-2xl border border-slate-700 bg-[#111827] p-5 shadow-2xl shadow-black/20 sm:p-7"><div className="mb-7 flex items-center justify-between"><div><p className="text-xs text-slate-500">Protection estimator</p><p className="mt-1 text-sm font-medium text-white">Build your starting point</p></div><div className="rounded-full bg-slate-800 px-3 py-1 text-[10px] text-cyan-200">Step 1 of 3</div></div>{submitted ? <Success /> : <><Fieldset label="Vehicle size">{sizes.map((item, i) => <button key={item.label} onClick={() => setSize(i)} className={`rounded-lg border p-3 text-left text-sm transition ${size === i ? 'border-cyan-300 bg-cyan-300/10 text-cyan-100' : 'border-slate-700 text-slate-400 hover:border-slate-500'}`}>{item.label}<span className="mt-1 block text-[10px] text-slate-500">{i === 0 ? 'Most sedans & coupes' : i === 1 ? '+$150 typical adjustment' : '+$250 typical adjustment'}</span></button>)}</Fieldset><Fieldset label="Protection objective">{goals.map((item, i) => <button key={item.label} onClick={() => setGoal(i)} className={`flex min-h-16 items-center rounded-lg border p-4 text-left text-sm font-medium leading-snug transition ${goal === i ? 'border-cyan-300 bg-cyan-300/10 text-cyan-100' : 'border-slate-700 text-slate-400 hover:border-slate-500'}`}>{item.label}</button>)}</Fieldset>{goal !== 1 && <button type="button" role="switch" aria-checked={addTint} onClick={() => setAddTint(v => !v)} className={`mb-6 flex w-full items-center justify-between gap-3 rounded-lg border p-3 text-left text-sm transition ${addTint ? 'border-cyan-300 bg-cyan-300/10 text-cyan-100' : 'border-slate-700 text-slate-400 hover:border-slate-500'}`}><span className="flex items-center gap-2"><Sun size={15} className="text-cyan-300" />{siteConfig.tintAddOn.label} <span className="text-slate-500">(+${siteConfig.tintAddOn.price})</span></span><span className={`grid size-5 place-items-center rounded-full border ${addTint ? 'border-cyan-300 bg-cyan-300 text-[#0a0d14]' : 'border-slate-600'}`}>{addTint && <Check size={12} />}</span></button>}<div className="my-7 rounded-xl border border-cyan-300/30 bg-[#0a0d14] p-5"><div className="flex items-end justify-between"><div><p className="text-xs text-slate-500">Estimated studio investment</p><p className="mt-1 text-3xl font-semibold tracking-tight text-white">${estimate.min.toLocaleString()} <span className="text-slate-600">–</span> ${estimate.max.toLocaleString()}<span className="text-sm text-slate-500">+</span></p></div><Droplets className="text-cyan-300" size={25} /></div><p className="mt-3 text-xs leading-5 text-slate-500">Starting range for {selected.name}. Final quote follows a light-tunnel inspection.</p></div><div className="grid gap-3 sm:grid-cols-3"><input aria-label="Vehicle year, make and model" placeholder="Year / make / model" className="rounded-lg border border-slate-700 bg-[#0d121c] px-3 py-3 text-sm text-white outline-none placeholder:text-slate-600 focus:border-cyan-300 sm:col-span-2" /><input aria-label="Your name" placeholder="Your name" className="rounded-lg border border-slate-700 bg-[#0d121c] px-3 py-3 text-sm text-white outline-none placeholder:text-slate-600" /></div><div className="mt-3 flex gap-3"><input aria-label="Phone number" placeholder="Mobile number for breakdown" className="min-w-0 flex-1 rounded-lg border border-slate-700 bg-[#0d121c] px-3 py-3 text-sm text-white outline-none placeholder:text-slate-600 focus:border-cyan-300" /><button onClick={() => setSubmitted(true)} className="rounded-lg bg-cyan-300 px-5 py-3 text-sm font-semibold text-[#0a0d14] transition hover:bg-cyan-200">Text my estimate</button></div><p className="mt-3 flex items-start gap-2 text-[11px] leading-5 text-slate-500"><ShieldCheck size={13} className="mt-0.5 shrink-0 text-cyan-300" />{siteConfig.contactReassurance}</p></>}</div></div></section>

    <section className="mx-auto max-w-6xl px-5 py-16 lg:px-8 lg:py-24"><div className="mb-10 flex flex-wrap items-end justify-between gap-5"><div><p className="mb-3 text-xs font-semibold uppercase tracking-[.25em] text-cyan-300">02 — The protection menu</p><h2 className="text-4xl font-semibold tracking-[-.04em] text-white sm:text-5xl">Specific by design.</h2></div><p className="max-w-xs text-sm leading-6 text-slate-500">No vague packages. Just clear scope, cure windows, and warranty coverage.</p></div><div className="grid gap-4 lg:grid-cols-3">{siteConfig.packages.map((item, i) => <article key={item.name} className={`relative rounded-2xl border p-6 ${item.badge ? 'border-cyan-300/50 bg-cyan-300/[.06]' : 'border-slate-800 bg-[#111827]'}`}>{item.badge && <span className="absolute -top-3 left-6 rounded-full bg-cyan-300 px-3 py-1 text-[10px] font-semibold uppercase tracking-[.12em] text-[#0a0d14]">{item.badge}</span>}<p className="text-[10px] uppercase tracking-[.2em] text-slate-500">{item.eyebrow}</p><h3 className="mt-7 min-h-14 text-xl font-semibold text-white">{item.name}</h3><p className="mt-4 text-3xl font-semibold text-cyan-300">${item.price.toLocaleString()}<span className="text-base text-slate-500">{item.suffix}</span></p><p className="mt-2 text-[11px] leading-5 text-slate-500">{siteConfig.priceNote}</p><p className="mt-4 min-h-16 text-sm leading-6 text-slate-400">{item.description}</p><ul className="mt-6 space-y-3 border-t border-slate-800 pt-5">{item.specs.map(spec => <li key={spec} className="flex gap-2 text-xs text-slate-300"><Check className="shrink-0 text-cyan-300" size={14} /> {spec}</li>)}</ul></article>)}</div></section>

    <section className="border-y border-slate-800 bg-[#0d121c] px-5 py-16 lg:px-8 lg:py-24"><div className="mx-auto grid max-w-6xl gap-10 lg:grid-cols-[.8fr_1.2fr]"><div><p className="mb-3 text-xs font-semibold uppercase tracking-[.25em] text-cyan-300">03 — Answers, not friction</p><h2 className="text-4xl font-semibold tracking-[-.04em] text-white">Questions deserve<br /><span className="text-slate-500">real answers.</span></h2></div><div className="divide-y divide-slate-800 rounded-2xl border border-slate-800 bg-[#111827] px-5">{siteConfig.faqs.map((item, i) => <div key={item.question}><button onClick={() => setFaq(faq === i ? null : i)} className="flex w-full items-center justify-between gap-5 py-5 text-left text-sm font-medium text-slate-200"><span>{item.question}</span>{faq === i ? <X size={16} className="shrink-0 text-cyan-300" /> : <ChevronDown size={16} className="shrink-0 text-slate-500" />}</button>{faq === i && <p className="max-w-2xl pb-5 text-sm leading-6 text-slate-400">{item.answer}</p>}</div>)}</div></div></section>

    <section className="mx-auto max-w-6xl px-5 py-16 lg:px-8 lg:py-20"><div className="rounded-2xl border border-cyan-300/30 bg-cyan-300/[.06] p-7 sm:p-10"><div className="grid gap-8 lg:grid-cols-[1fr_auto] lg:items-center"><div><div className="mb-5 flex items-center gap-3"><div className="grid size-10 place-items-center rounded-full bg-cyan-300 text-[#0a0d14]"><ShieldCheck size={21} /></div><p className="text-xs font-semibold uppercase tracking-[.2em] text-cyan-200">The clean handoff guarantee</p></div><h2 className="max-w-2xl text-3xl font-semibold tracking-[-.03em] text-white sm:text-4xl">You only pay when the finish is flawless.</h2><p className="mt-4 max-w-xl text-sm leading-6 text-slate-400">Before your keys are handed back, we inspect every panel under color-match inspection lights together. No surprises. No rushed handoff.</p></div><a href="#estimator" className="rounded-lg bg-cyan-300 px-5 py-3 text-center text-sm font-semibold text-[#0a0d14]">Hold my spot <ArrowRight className="ml-2 inline" size={16} /></a></div></div></section>

    <footer className="border-t border-slate-800 px-5 py-10 lg:px-8"><div className="mx-auto flex max-w-6xl flex-col justify-between gap-5 text-xs text-slate-500 sm:flex-row"><div><p className="font-semibold text-slate-300">{siteConfig.shopName} <span className="font-normal text-slate-600">/ {siteConfig.descriptor}</span></p><p className="mt-2 flex items-center gap-2"><MapPin size={13} /> {siteConfig.location}</p></div><div className="flex gap-5 sm:items-end"><span className="flex items-center gap-2"><Clock3 size={13} /> Mon–Sat · 8am–6pm</span><span>{siteConfig.phone}</span></div></div></footer>
  </main>
}

function Trust({ icon, text }: { icon: React.ReactNode, text: string }) { return <div className="flex items-center gap-3 text-xs text-slate-400">{<span className="text-cyan-300">{icon}</span>}{text}</div> }
function Fieldset({ label, children }: { label: string, children: React.ReactNode }) { return <fieldset className="mb-6"><legend className="mb-3 text-xs font-medium text-slate-300">{label}</legend><div className="grid grid-cols-1 gap-3 sm:grid-cols-3">{children}</div></fieldset> }
function Success() { return <div className="py-8 text-center"><div className="mx-auto grid size-14 place-items-center rounded-full bg-cyan-300 text-[#0a0d14]"><Check size={28} /></div><h3 className="mt-5 text-2xl font-semibold text-white">Your estimate is on its way.</h3><p className="mx-auto mt-3 max-w-sm text-sm leading-6 text-slate-400">We just texted your detailed breakdown and current bay openings. Marcus will follow up shortly.</p><div className="mx-auto mt-7 max-w-xs rounded-2xl border border-slate-700 bg-[#0a0d14] p-4 text-left"><p className="text-[10px] text-slate-500">MESSAGES · NOW</p><p className="mt-3 text-xs leading-5 text-slate-300">Hey there, Marcus here from the bay. Got your request. We have 2 coating bays open this Thursday. Here is your preliminary estimate breakdown…</p></div></div> }
